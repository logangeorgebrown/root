# ROOT requirements for third-party Python packages

# PyROOT: Interoperability with numpy arrays
# TMVA: PyMVA interfaces
numpy>=1.21.0rc1

# PyROOT: ROOT.Numba.Declare decorator
numba>=0.49.0 ; python_version < "3.11" # See https://github.com/numba/numba/issues/8304
cffi>=1.9.1

# Notebooks: ROOT C++ kernel
notebook>=6.4.12
metakernel>=0.20.0

# Distributed RDataFrame
pyspark>=3.2.2 # Spark backend
dask>=2022.08.1 ; python_version >= "3.8" # Dask backend
distributed>=2022.08.1 ; python_version >= "3.8" # Dask backend
ipython>=7.16.3 # not directly required, pinned by Snyk to avoid a vulnerability
jinja2>=3.1.3 # not directly required, pinned by Snyk to avoid a vulnerability
jupyter-core>=4.11.2 # not directly required, pinned by Snyk to avoid a vulnerability
mistune>=2.0.3 # not directly required, pinned by Snyk to avoid a vulnerability
nbconvert>=6.3.0b0 # not directly required, pinned by Snyk to avoid a vulnerability
prompt-toolkit>=3.0.13 # not directly required, pinned by Snyk to avoid a vulnerability
pygments>=2.15.0 # not directly required, pinned by Snyk to avoid a vulnerability
